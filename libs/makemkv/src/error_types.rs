pub enum BackupError {
    DataError,
}

pub enum InfoError {
    DataError,
}

pub enum MkvError {
    DataError,
}

pub enum FirmwareError {
    DataError,
}

pub enum LicenseError {
    DataError,
}
